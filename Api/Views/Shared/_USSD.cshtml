<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>
    <!-- JavaScript -->
    <script src="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/alertify.min.js"></script>

    <!-- CSS -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/alertify.min.css" />
    <!-- Default theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/default.min.css" />
    <!-- Semantic UI theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/semantic.min.css" />
    <!-- Bootstrap theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/bootstrap.min.css" />

    <!--
        RTL version
    -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/alertify.rtl.min.css" />
    <!-- Default theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/default.rtl.min.css" />
    <!-- Semantic UI theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/semantic.rtl.min.css" />
    <!-- Bootstrap theme -->
    <link rel="stylesheet" href="//cdn.jsdelivr.net/npm/alertifyjs@1.11.2/build/css/themes/bootstrap.rtl.min.css" />

    <style>
    .phone {
        border: 40px solid black;
        border-width: 55px 7px;
        border-radius: 40px;
        margin: 50px auto;
        overflow: hidden;
        transition: all 0.5s ease;
    }

        .button {
            background-color: #4CAF50; /* Green */
            border: none;
            color: white;
            padding: 5px 10px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 12px;
            margin: 4px 2px;
            cursor: pointer;
        }

        .button1 {
            background-color: white;
            color: black;
            border: 2px solid #008CBA;
        }
        pre {
            display: block;
            font-family: monospace;
            white-space: pre;
            margin: 0em 0;
        } 

    </style>
    <script>
        function StartSessioniDoBetBenin(id)
        {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/idoBetBenin/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {
                    
                });
            
        }

        function StartSessionBtoBetBenin(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/GreenWinBtoBet/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessionBtoBetMTNCameroon(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNCameroonBtoBet/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNCameroon(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNCameroon/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetCongo_STG(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;

                    window.location.href = "/USSDEmulator/CongoB_STG/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNCongo(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;

                    window.location.href = "/USSDEmulator/MTNCongoB/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNCongo142(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;

                    window.location.href = "/USSDEmulator/MTNCongoB142/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn
                },
                function () {

                });

        }

        function StartSessioniDoBetAirTelCongo142(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;

                    window.location.href = "/USSDEmulator/AirTelCongoB142/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNGuinea(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNGuineaiDoBet/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNGuinea142(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNGuineai142/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNNG(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNNGiDoBet/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessionMTNIC479(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNIC479/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }


        function StartSessionYDGamesBenin(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/YDGamesBenin/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNLNBBenin(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNLNBBenin/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMTNLNBBeninV142(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MTNLNBBeninV142/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMoovLNBBeninV142(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MoovLNBBeninV142/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        function StartSessioniDoBetMoovLNBBenin(id) {
            alertify.confirm("You are about to start a new session! ",
                function () {
                    var w = document.getElementById("WIT").value;
                    var h = document.getElementById("HIT").value;
                    var msisdn = document.getElementById("MIT").value;
                    window.location.href = "/USSDEmulator/MoovLNBBenin/" + id + "?width_p=" + w + "&height_p=" + h + "&MSISDN=" + msisdn;
                },
                function () {

                });

        }

        
 
        // Set the date we're counting down to
        var countDownDate = new Date().getTime() + 1000 * 60 * 2;
        // Update the count down every 1 second
        var x = setInterval(function () {

            // Get todays date and time
            var now = new Date().getTime();

            // Find the distance between now and the count down date
            var distance = countDownDate - now;

            // Time calculations for days, hours, minutes and seconds
            var minutes = Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60));
            var seconds = Math.floor((distance % (1000 * 60)) / 1000);

            // Output the result in an element with id="demo"
            document.getElementById("timer").innerHTML = minutes + "m " + seconds + "s ";

            // If the count down is over, write some text 
            if (distance < 0) {
                clearInterval(x);
                document.getElementById("timer").innerHTML = "Session Expired";

            }
        }, 1000);
        
        
        
        
    </script>

    

</head>
<body style="background-color:#09203f;">
    
        @RenderBody()
    
</body>
</html>
